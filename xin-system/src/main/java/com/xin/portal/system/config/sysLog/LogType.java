package com.xin.portal.system.config.sysLog;

public enum LogType {
		add,
		delete,
		update,
		query,
		login,
		logout,
		openfile,
		upload,
		download,
		collect,
		sync,
		importData,
		export
}
